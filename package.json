{
  "name": "healthcare-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@healthcare-monorepo/shared && npm run build --workspaces --if-present",
    "dev": "concurrently \"npm run dev --workspace=@healthcare-monorepo/shared\" \"npm run dev --workspace=@healthcare-monorepo/client\" \"npm run dev --workspace=@healthcare-monorepo/server\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server":"npm run dev --workspace=server",
    "db:generate": "npm run db:generate --workspace=@healthcare/database",
    "db:push": "npm run db:push --workspace=@healthcare/database",
    "db:seed": "npm run db:seed --workspace=@healthcare/database"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
